import styled from "styled-components";

const StyledBio = styled.section`
  
`;

const Bio = ({ title, bio }) => {
  return (
    <StyledBio>
      <h1>Eu crio interfaces</h1>
    </StyledBio>
  );
};

export default Bio;